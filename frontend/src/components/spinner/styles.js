import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  spinner: {
    height: '4rem',
    width: '4rem'
  },
  spinnerWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%'
  }
}));
